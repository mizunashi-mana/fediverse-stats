{
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "main": "src/index.js",
  "scripts": {
    "start": "npm run build && node .",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^20.8.6",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.5.1",
    "commander": "^11.1.0"
  }
}
